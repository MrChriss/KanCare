<%= month_calendar events: @care_actions do |date, care_actions| %>
  <%= date.day %>

  <% care_actions.each do |care_action| %>
    <%= link_to render_care_action_icon(care_action), care_action_path(care_action.id) %>
  <% end %>
<% end %>

<%= link_to 'add new', new_care_action_path %>
